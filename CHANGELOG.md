# Changelog

## Unreleased

- Generate the browser demo transcript from the release CLI and verify exact
  transcript fidelity in a fresh browser.
- Add the three-step Firebase project workflow to the product page.
- Recheck all 25 public claims, responsive behavior, routing, privacy, and
  accessibility from a clean clone and the deployed site.
- Pass strict Clippy checks without changing project-selection precedence.
- Add immutable caching for content-hashed site assets.
- Add CSP, framing, permissions, and one-year HSTS response policies.
- Add deployment-policy and strict type/lint regression gates.

All notable changes to this project are documented here.

## 0.1.0 — 2026-08-28

- Add the read-only Firebase environment diagnostic CLI.
- Add offline project, emulator, rules, CLI, and credential checks.
- Add opt-in Firebase authentication and project reachability checks.
- Add stable JSON output, CI mode, and documented exit codes.
- Add the static product site and local interactive diagnosis examples.
